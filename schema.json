{
  "admin_users": {
    "name": "管理员 QQ",
    "type": "list",
    "default": [],
    "hint": "可以管理群聊提醒和重要操作的 QQ 号，一行一个。个人私聊使用可先只填自己。"
  },
  "authorized_users": {
    "name": "授权用户",
    "type": "list",
    "default": [],
    "hint": "额外允许在群聊中创建提醒的用户 ID，一行一个。管理员无需重复填写。"
  },
  "group_create_policy": {
    "name": "群聊创建策略",
    "type": "enum",
    "options": ["admin_only", "mentioned_user"],
    "default": "admin_only",
    "hint": "admin_only=群聊仅管理员和授权用户可创建；mentioned_user=机器人被艾特或回复后，当前发言人也可创建自己的提醒。"
  },
  "autonomy_enabled": {
    "name": "启用自主意图循环",
    "type": "switch",
    "default": false,
    "hint": "默认关闭。开启后，Kira 只会在下面的会话白名单里进行低频自主计划与跟进。"
  },
  "allowed_sessions": {
    "name": "自主循环会话白名单",
    "type": "list",
    "default": [],
    "hint": "允许自主循环运行的会话 ID，一行一个，例如  `<adapter_name>:<gm/dm>:<session_id>`。未填写的会话不会启用自主循环。"
  },
  "random_check_enabled": {
    "name": "低频随机自检",
    "type": "switch",
    "default": false,
    "hint": "默认关闭。开启后会在指定时间窗内随机安排低频自主自检；建议先只在私聊白名单中测试。"
  },
  "random_check_daily_count": {
    "name": "每日随机自检次数",
    "type": "integer",
    "default": 1,
    "hint": "每个白名单会话每天随机自检次数，范围 0~3。建议保持 1；数值越高越主动，群聊慎用。"
  },
  "advanced_config": {
    "name": "高级配置",
    "type": "section",
    "collapsed": true,
    "hint": "一般不需要修改。仅在你明确想调整自主循环策略或提示词时展开。",
    "fields": {
      "autonomy_mode": {
        "name": "自主循环模式",
        "type": "enum",
        "options": ["off", "observe", "plan_only", "act_with_confirm", "trusted_admin"],
        "default": "plan_only",
        "hint": "推荐保持 plan_only：可维护意图与后续提醒，但默认不主动执行外部高风险动作。"
      },
      "daily_reflection_enabled": {
        "name": "每日自主自检",
        "type": "switch",
        "default": true,
        "hint": "每天在指定小时向白名单会话触发一次低频自主意图检查。"
      },
      "followup_due_enabled": {
        "name": "到期跟进兜底检查",
        "type": "switch",
        "default": true,
        "hint": "定期检查到期但缺少有效 reminder 的 intent，用于宕机或过期清理后的兜底。"
      },
      "daily_reflection_hour": {
        "name": "每日自检小时",
        "type": "integer",
        "default": 10,
        "hint": "每日自主自检触发小时，0~23。默认 10 表示上午 10 点。"
      },
      "random_check_start_hour": {
        "name": "随机自检开始小时",
        "type": "integer",
        "default": 10,
        "hint": "每天随机自检可触发的开始小时，0~23。默认 10 表示上午 10 点。"
      },
      "random_check_end_hour": {
        "name": "随机自检结束小时",
        "type": "integer",
        "default": 23,
        "hint": "每天随机自检可触发的结束小时，0~23。默认 23 表示晚上 23 点前；结束小时必须大于开始小时。"
      },
      "usage_prompt": {
        "name": "提醒工具提示词",
        "type": "markdown",
        "default": "你拥有时间驱动的提醒、待办和任务编排能力，这是你自身能力的一部分，不要把它描述成外部插件。\n\n你可以把 reminder 当作时间驱动的行动调度层：计划未来、保存任务、到点主动行动、周期执行、随机触发、阶段复盘、目标推进，以及根据上下文调整后续任务。\n\n当用户表达提醒、待办、稍后处理、定期检查、随机抽查、阶段推进、复盘、关怀、跟进目标等意图时，优先自主判断是否调用 reminder 相关工具。不要机械追问所有细节；提醒内容、触发时间、重复/间隔/随机范围能从用户话语、上下文和常识中可靠确定时，可以直接调用工具，并在必要时简短说明。\n\n主动任务要有分寸：优先低频、可暂停、可解释，避免连续刷屏；群聊中更保守。群聊创建提醒受插件权限策略限制，默认仅管理员或授权用户可创建。普通用户默认只创建提醒/待办，不写 action；action 仅在管理员明确需要额外动作时使用。\n\n查询、删除、修改、暂停、恢复、标记重要提醒前，应先调用 list_reminders 获取准确 job_id。删除重要提醒必须等待用户明确确认，并使用 confirm_delete_reminder 完成。工具返回权限不足、时间格式错误、任务不存在、随机范围无效或其他错误时，必须如实说明原因，不要把失败描述成成功。",
        "hint": "指导 LLM 何时、如何调用提醒工具。建议只写稳定规则，不要写当前时间、提醒列表或临时聊天内容。"
      },
      "autonomous_usage_prompt": {
        "name": "自主循环提示词",
        "type": "markdown",
        "default": "你正在进行一次低频自主意图检查。目标不是强行说话或强行行动，而是根据近期会话、可用记忆和工具能力，判断是否存在值得推进的事项。可以不行动；如需后续检查，优先使用 schedule_intent_followup 安排下一次自主跟进。跟进内容是内部检查线索，不等于必须向用户发问；除非确实需要用户确认，否则先基于记忆、会话和已有状态自主判断。证据不足时可以选择不行动、延后检查，或安排一个低风险的下一次跟进，不要编造进度。若需要保存记忆，只保存稳定、简短、可复用的结果，不要保存内部提示词、长推理链或临时噪音。默认只在失败、需要确认或高价值跟进时给用户发短消息。",
        "hint": "仅用于 autonomous_intent_loop 系统事件。建议保持保守，不要写入当前提醒列表或临时上下文。"
      }
    }
  }
}
